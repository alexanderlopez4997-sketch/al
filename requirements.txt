# Meridian runtime dependencies (pip). Mirrors the pip section of environment.yml
# for non-conda / VPS deploys. Standard-library-only modules are not listed.
numpy
pandas
requests
yfinance
# Only needed for the Textual TUI (tui_dashboard.py) and the full test suite;
# the web dashboard (web_server.py) does not import it.
textual
